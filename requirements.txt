Flask>=3.0
cryptography>=41.0
Werkzeug>=3.0
PyYAML>=6.0
paramiko>=3.0
requests>=2.28
# Audit H-1: defusedxml is a HARD requirement. nat_lookup parses untrusted
# XML responses from network firewalls — the stdlib xml.etree parser is
# vulnerable to billion-laughs / XXE attacks.
defusedxml>=0.7.1
# Wave-7.2: python-dotenv lets the Flask CLI auto-load `.env` from the repo
# root so operators can put PERGEN_API_TOKEN / PERGEN_DEV_OPEN_API there
# without sourcing it manually each time. run.sh ALSO loads .env as a
# fallback so this dependency is belt-and-suspenders, not strictly
# required to boot.
python-dotenv>=1.0
